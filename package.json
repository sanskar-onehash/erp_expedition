{
  "name": "expedition",
  "private": true,
  "description": "Wrapper package.json so `bench build --app expedition` invokes the Vite build.",
  "scripts": {
    "build": "cd frontend && npm run build"
  }
}
